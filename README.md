# Rook QA Sandbox

Scratch project for testing Rook's coding capabilities.

## Known Issues
- `parseUserInput` crashes on null input
- `validateEmail` uses weak regex
- No authentication middleware
- Missing error handling in routes
